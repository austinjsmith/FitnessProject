<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/home.css">
    <script type = "text/javascript" src="/js/addmeal.js"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home</title>
</head>
<body>
    <div class="topbar">
        <a href="#home">Home</a>
        <a href="#trainer">My Trainer</a>
        <a href="index.html">Calorie Counter</a>
        <a href= "home">Meal Log</a>
        <a href= "tdee">Calculator</a>
        <a href="login.html">Log In</a>
    </div>
    <table >
        <tr id="rows">
            <th>Meal Name</th>
            <th>Meal Calories</th>
            <th>Fats</th>
            <th>Proteins</th>
            <th>Carbs</th>
        </tr>
        <tbody>
            <% if (meals.length) { %>
                <% for (const meal of meals){ %>   
                    <tr>
                        <td><%= meal.mealname %></td>
                        <td><%= meal.maincalorie %></td>
                        <td><%= meal.fats %></td>
                        <td><%= meal.carbs %></td>
                        <td><%= meal.proteins %></td>
                        <br>
                        <% } %>
            <% } %>
            <% if (!meals.length) { %>
                <br>
                <p>your meal log is empty click <a href="login.html">here </a> to sign in to add a meal</p>
            <% } %>
                </tr>
            </tbody>
        </table>
</body>
</html>